# Root example env — copy into each service .env from respective .env.example files

# Postgres (Neon credentials for prod — use Neon for production)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=shopify_ai

# Redis
REDIS_URL=redis://redis:6379

# Backend
BACKEND_PORT=4000
BACKEND_NODE_ENV=development
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/shopify_ai
REDIS_URL=redis://redis:6379
JWT_SECRET=change_this_to_secure

# AI
AI_PORT=3002
AI_NODE_ENV=development
AI_SECRET=change_ai_secret

# WhatsApp
WA_PORT=3001
WA_NODE_ENV=development

# Frontend
NEXT_PUBLIC_API_BASE=http://localhost:4000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
